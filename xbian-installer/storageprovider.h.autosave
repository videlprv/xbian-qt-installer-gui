#ifndef STORAGEPROVIDER_H
#define STORAGEPROVIDER_H

#include <QObject>

class StorageProvider : public QObject
{
    Q_OBJECT
public:
    explicit StorageProvider(QObject *parent = 0);
    
signals:

public slots:
    void checkAvaliableStorageMedia();
};

#endif // STORAGEPROVIDER_H
